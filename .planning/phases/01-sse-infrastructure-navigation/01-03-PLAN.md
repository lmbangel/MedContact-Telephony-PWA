---
phase: 01-sse-infrastructure-navigation
plan: 03
type: execute
wave: 2
depends_on: ["01-02"]
files_modified:
  - app/dashboard-home.html
  - app/src/dashboard-home.js
  - app/dashboard-stats.html
autonomous: false

must_haves:
  truths:
    - "User can click stats icon in sidebar to navigate to stats page"
    - "Stats icon in sidebar is clickable on all dashboard pages"
    - "Navigation maintains consistent sidebar across pages"
    - "Stats icon shows active state when on stats page"
    - "Memory usage stays stable over extended session"
  artifacts:
    - path: "app/dashboard-home.html"
      provides: "Stats icon with id and click handler wiring"
      contains: "id=\"side-stats-icon\""
    - path: "app/src/dashboard-home.js"
      provides: "Click handler for stats navigation"
      contains: "side-stats-icon"
  key_links:
    - from: "app/src/dashboard-home.js"
      to: "/app/dashboard-stats.html"
      via: "window.location.href"
      pattern: "dashboard-stats\\.html"
---

<objective>
Wire up sidebar navigation so users can click the stats icon to navigate to the stats page.

Purpose: Complete the navigation requirement (NAV-01) by making the existing stats icon in the sidebar functional. Users need a clear, consistent way to access the stats page from any dashboard page.

Output: Clickable stats icon in sidebar that navigates to stats page, with proper active state indication on the destination page.
</objective>

<execution_context>
@/home/lmbangel/.claude/get-shit-done/workflows/execute-plan.md
@/home/lmbangel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

# Files created in Plan 02
@app/dashboard-stats.html
@app/src/dashboard-stats.js

# Files to modify
@app/dashboard-home.html
@app/src/dashboard-home.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add stats icon id and click handler in dashboard-home</name>
  <files>app/dashboard-home.html, app/src/dashboard-home.js</files>
  <action>
Modify dashboard-home.html:

1. Find the stats icon button (around line 206, the one with the bar chart SVG):
```html
<!-- Stats Icon -->
<button class="w-10 h-10 flex items-center justify-center text-white hover:bg-white/10 rounded-lg transition">
```

2. Add id="side-stats-icon" to this button:
```html
<button id="side-stats-icon" class="w-10 h-10 flex items-center justify-center text-white hover:bg-white/10 rounded-lg transition">
```

Modify dashboard-home.js:

3. In the setupEventListeners function (around line 143), add a click handler for the stats icon:
```javascript
// Stats page navigation
const statsBtn = document.getElementById('side-stats-icon');
statsBtn?.addEventListener('click', handleNavigateToStats);
```

4. Add the handler function (can be placed near handleLogout around line 477):
```javascript
/**
 * Handle navigation to stats page
 */
function handleNavigateToStats() {
  window.location.href = './dashboard-stats.html';
}
```

IMPORTANT: This is a multi-page app (not SPA), so use window.location.href for navigation. Do NOT try to implement SPA routing.
  </action>
  <verify>
1. Check dashboard-home.html has id="side-stats-icon" on the stats button:
   `grep -n "side-stats-icon" app/dashboard-home.html`
2. Check dashboard-home.js has event listener for 'side-stats-icon':
   `grep -n "side-stats-icon" app/src/dashboard-home.js`
3. Check dashboard-home.js has handleNavigateToStats function:
   `grep -n "handleNavigateToStats" app/src/dashboard-home.js`
4. Manual test: Load dashboard-home.html, click stats icon, should navigate to dashboard-stats.html
  </verify>
  <done>
Stats icon in dashboard-home sidebar has id and click handler that navigates to dashboard-stats.html.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add dashboard icon navigation in stats page</name>
  <files>app/dashboard-stats.html, app/src/dashboard-stats.js</files>
  <action>
Ensure dashboard-stats.html has proper navigation back to dashboard:

1. Verify dashboard-stats.html has id="side-dashboard-icon" on the dashboard/home icon (should already be there from copying dashboard-home.html structure)

2. In dashboard-stats.js, add navigation handler for dashboard icon. Add after the initializeStatsPage function:
```javascript
// Setup sidebar navigation
function setupSidebarNavigation() {
  const dashboardBtn = document.getElementById('side-dashboard-icon');
  dashboardBtn?.addEventListener('click', () => {
    cleanup(); // Clean up SSE before navigating
    window.location.href = './dashboard-home.html';
  });
}
```

3. Call setupSidebarNavigation() in initializeStatsPage() after setting up SSE.

IMPORTANT: Call cleanup() before navigating away to properly close SSE connection. This prevents memory leaks from orphaned EventSource connections.
  </action>
  <verify>
1. Check dashboard-stats.js calls cleanup() before navigation:
   `grep -n "cleanup()" app/src/dashboard-stats.js` - should show cleanup call in navigation handler
2. Check setupSidebarNavigation function exists:
   `grep -n "setupSidebarNavigation" app/src/dashboard-stats.js` - should show function definition and call
3. Manual test: Load dashboard-stats.html, click dashboard icon, should navigate to dashboard-home.html
4. Check browser console for any SSE connection errors after navigation
  </verify>
  <done>
Dashboard icon in stats page sidebar navigates back to dashboard-home with proper SSE cleanup.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete navigation flow between dashboard and stats pages:
1. Stats icon in dashboard-home sidebar navigates to stats page
2. Dashboard icon in stats page sidebar navigates back to dashboard
3. SSE connection properly cleans up when navigating away
4. Role-based access control redirects unauthorized users
  </what-built>
  <how-to-verify>
Prerequisites: Backend must be running (from Plan 01) and you must be logged in.

1. Start the backend: `cd api && go run main.go`
2. Start the frontend: `cd app && npm run dev`
3. Open browser to http://localhost:5173/app/dashboard-home.html
4. Log in with a user that has admin/manager/supervisor/support role

Test NAV-01 (Stats page access):
5. Click the stats icon (bar chart) in the left sidebar
6. Should navigate to dashboard-stats.html
7. Stats page should show "Team Statistics" heading
8. SSE status indicator should show "Connected" (green dot)
9. Debug panel should show heartbeat timestamps updating every 30 seconds

Test navigation back:
10. Click the dashboard icon (house) in the left sidebar
11. Should navigate back to dashboard-home.html
12. No console errors about orphaned SSE connections

Test NAV-02 (Unauthorized redirect):
13. If possible, log in as a regular "agent" role user
14. Try to access http://localhost:5173/app/dashboard-stats.html directly
15. Should be redirected to dashboard-home.html

Test memory stability (Success Criteria #5):
16. Open browser DevTools -> Performance tab (or Memory tab)
17. Take initial memory snapshot (note JS Heap size)
18. Stay on stats page for 5 minutes (observing heartbeats)
19. Navigate to dashboard-home and back to stats 5 times
20. Take final memory snapshot
21. Memory should be stable (not growing significantly, under 200MB for the tab)

Alternative memory test using Chrome Task Manager:
- Open Chrome menu -> More Tools -> Task Manager
- Note initial memory for the tab
- Repeat navigation 10 times
- Memory should stay stable (no continuous growth pattern)

Report any issues with:
- Navigation not working
- SSE not connecting
- Wrong redirect behavior
- Console errors
- Memory growing continuously (indicates leak)
  </how-to-verify>
  <resume-signal>Type "approved" if all navigation, SSE connections, and memory stability checks pass, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
All verification is covered in the checkpoint task above, which tests the full Phase 1 requirements:
- NAV-01: Stats page accessible via side-stats-icon button click
- NAV-02: Unauthorized roles redirected away from stats page
- REAL-01: SSE connection establishes and receives heartbeat pings
- REAL-02: SSE reconnection logic implemented (can test by stopping/starting backend)
- Memory stability: Tab memory stays stable over extended use and navigation cycles
</verification>

<success_criteria>
- [ ] Stats icon in dashboard-home.html has id="side-stats-icon"
- [ ] Clicking stats icon navigates to dashboard-stats.html
- [ ] Dashboard icon in stats page navigates back with SSE cleanup
- [ ] Human verification confirms full navigation flow works
- [ ] Human verification confirms SSE connection and heartbeats work
- [ ] Human verification confirms role-based redirect works
- [ ] Human verification confirms memory stays stable over 5+ minute session
</success_criteria>

<output>
After completion, create `.planning/phases/01-sse-infrastructure-navigation/01-03-SUMMARY.md`
</output>
