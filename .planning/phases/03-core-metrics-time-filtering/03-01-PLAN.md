---
phase: 03-core-metrics-time-filtering
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/schema.sql
  - api/queries.sql
  - api/db/queries.sql.go
autonomous: true

must_haves:
  truths:
    - "Database queries with date filters complete in under 1 second with 10k+ records"
    - "API can return call stats filtered by today, yesterday, this week, this month, custom range"
    - "API can return task stats filtered by today, yesterday, this week, this month, custom range"
  artifacts:
    - path: "api/schema.sql"
      provides: "Composite indexes on transcriptions and tasks for time-filtered queries"
      contains: "idx_transcriptions_company_created"
    - path: "api/queries.sql"
      provides: "Time-filtered call and task stat queries for all roles"
      contains: "GetCallStatsByCompanyToday"
  key_links:
    - from: "api/queries.sql"
      to: "api/db/queries.sql.go"
      via: "sqlc code generation"
      pattern: "GetCallStatsByCompany(Today|Yesterday|ThisWeek|ThisMonth|Range)"
---

<objective>
Add database indexes and time-filtered SQL queries for call and task metrics.

Purpose: Enable sub-1-second performance for time-filtered stats queries on 10k+ records by adding composite indexes and creating separate queries for each time filter variant (today, yesterday, this week, this month, custom range).

Output: Database schema with performance indexes and sqlc queries for time-filtered call/task stats across all roles (admin, manager, support, agent).
</objective>

<execution_context>
@/home/lmbangel/.claude/get-shit-done/workflows/execute-plan.md
@/home/lmbangel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-metrics-time-filtering/03-RESEARCH.md
@.planning/phases/02-role-based-data-layer/02-01-SUMMARY.md

# Existing queries to extend
@api/queries.sql
@api/schema.sql
</context>

<tasks>

<task type="auto">
  <name>Add composite indexes for time-filtered queries</name>
  <files>api/schema.sql</files>
  <action>
Add composite indexes to support efficient time-range filtering on call and task queries.

Add to api/schema.sql after existing transcriptions table indexes:

```sql
-- Composite index for time-filtered call queries (company + date range)
CREATE INDEX idx_transcriptions_company_created
  ON transcriptions (company_id, created_at)
  COMMENT 'For filtering calls by company and date range';

-- Composite index for time-filtered call queries (agent + date range)
CREATE INDEX idx_transcriptions_agent_created
  ON transcriptions (agent_id, created_at)
  COMMENT 'For filtering calls by agent and date range';

-- Composite index for time-filtered task queries (assigned_to + date range)
CREATE INDEX idx_tasks_assigned_created
  ON tasks (assigned_to, created_at)
  COMMENT 'For filtering tasks by assignee and date range';
```

These indexes follow the equality-before-range pattern (company_id/agent_id/assigned_to before created_at) as documented in research for MySQL composite index optimization.
  </action>
  <verify>
Run: `grep -A 2 "idx_transcriptions_company_created\|idx_transcriptions_agent_created\|idx_tasks_assigned_created" api/schema.sql`

Should show all three new indexes with COMMENT clauses.
  </verify>
  <done>
api/schema.sql contains three new composite indexes following MySQL optimization patterns (equality columns before range columns).
  </done>
</task>

<task type="auto">
  <name>Create time-filtered call stat queries for all roles</name>
  <files>api/queries.sql, api/db/queries.sql.go</files>
  <action>
Add time-filtered variants of call stat queries following the research pattern of separate queries per filter type (not dynamic SQL).

Add after existing GetCallStatsByCompany query in api/queries.sql:

**Admin/Support role queries (company-scoped):**

```sql
-- name: GetCallStatsByCompanyToday :one
SELECT
    COUNT(*) as total_calls,
    COUNT(CASE WHEN call_status = 'completed' THEN 1 END) as answered_calls,
    COUNT(CASE WHEN call_status IN ('no-answer', 'busy', 'failed') THEN 1 END) as missed_calls,
    COALESCE(AVG(CASE WHEN duration > 0 THEN duration END), 0) as avg_duration
FROM transcriptions
WHERE company_id = ? AND DATE(created_at) = CURDATE();

-- name: GetCallStatsByCompanyYesterday :one
SELECT
    COUNT(*) as total_calls,
    COUNT(CASE WHEN call_status = 'completed' THEN 1 END) as answered_calls,
    COUNT(CASE WHEN call_status IN ('no-answer', 'busy', 'failed') THEN 1 END) as missed_calls,
    COALESCE(AVG(CASE WHEN duration > 0 THEN duration END), 0) as avg_duration
FROM transcriptions
WHERE company_id = ? AND DATE(created_at) = DATE_SUB(CURDATE(), INTERVAL 1 DAY);

-- name: GetCallStatsByCompanyThisWeek :one
SELECT
    COUNT(*) as total_calls,
    COUNT(CASE WHEN call_status = 'completed' THEN 1 END) as answered_calls,
    COUNT(CASE WHEN call_status IN ('no-answer', 'busy', 'failed') THEN 1 END) as missed_calls,
    COALESCE(AVG(CASE WHEN duration > 0 THEN duration END), 0) as avg_duration
FROM transcriptions
WHERE company_id = ? AND YEARWEEK(created_at, 1) = YEARWEEK(CURDATE(), 1);

-- name: GetCallStatsByCompanyThisMonth :one
SELECT
    COUNT(*) as total_calls,
    COUNT(CASE WHEN call_status = 'completed' THEN 1 END) as answered_calls,
    COUNT(CASE WHEN call_status IN ('no-answer', 'busy', 'failed') THEN 1 END) as missed_calls,
    COALESCE(AVG(CASE WHEN duration > 0 THEN duration END), 0) as avg_duration
FROM transcriptions
WHERE company_id = ? AND YEAR(created_at) = YEAR(CURDATE()) AND MONTH(created_at) = MONTH(CURDATE());

-- name: GetCallStatsByCompanyRange :one
SELECT
    COUNT(*) as total_calls,
    COUNT(CASE WHEN call_status = 'completed' THEN 1 END) as answered_calls,
    COUNT(CASE WHEN call_status IN ('no-answer', 'busy', 'failed') THEN 1 END) as missed_calls,
    COALESCE(AVG(CASE WHEN duration > 0 THEN duration END), 0) as avg_duration
FROM transcriptions
WHERE company_id = ? AND created_at >= ? AND created_at < ?;
```

**Manager role queries (hierarchy-scoped):**

```sql
-- name: GetCallStatsByManagerToday :one
WITH RECURSIVE subordinates AS (
    SELECT u.id FROM users u WHERE u.reports_to = ? AND u.company_id = ? AND u.is_active = 1
    UNION ALL
    SELECT u.id FROM users u INNER JOIN subordinates s ON u.reports_to = s.id WHERE u.company_id = ? AND u.is_active = 1
)
SELECT
    COUNT(*) as total_calls,
    COUNT(CASE WHEN call_status = 'completed' THEN 1 END) as answered_calls,
    COUNT(CASE WHEN call_status IN ('no-answer', 'busy', 'failed') THEN 1 END) as missed_calls,
    COALESCE(AVG(CASE WHEN duration > 0 THEN duration END), 0) as avg_duration
FROM transcriptions
WHERE agent_id IN (SELECT id FROM subordinates) AND DATE(created_at) = CURDATE();

-- name: GetCallStatsByManagerYesterday :one
WITH RECURSIVE subordinates AS (
    SELECT u.id FROM users u WHERE u.reports_to = ? AND u.company_id = ? AND u.is_active = 1
    UNION ALL
    SELECT u.id FROM users u INNER JOIN subordinates s ON u.reports_to = s.id WHERE u.company_id = ? AND u.is_active = 1
)
SELECT
    COUNT(*) as total_calls,
    COUNT(CASE WHEN call_status = 'completed' THEN 1 END) as answered_calls,
    COUNT(CASE WHEN call_status IN ('no-answer', 'busy', 'failed') THEN 1 END) as missed_calls,
    COALESCE(AVG(CASE WHEN duration > 0 THEN duration END), 0) as avg_duration
FROM transcriptions
WHERE agent_id IN (SELECT id FROM subordinates) AND DATE(created_at) = DATE_SUB(CURDATE(), INTERVAL 1 DAY);

-- name: GetCallStatsByManagerThisWeek :one
WITH RECURSIVE subordinates AS (
    SELECT u.id FROM users u WHERE u.reports_to = ? AND u.company_id = ? AND u.is_active = 1
    UNION ALL
    SELECT u.id FROM users u INNER JOIN subordinates s ON u.reports_to = s.id WHERE u.company_id = ? AND u.is_active = 1
)
SELECT
    COUNT(*) as total_calls,
    COUNT(CASE WHEN call_status = 'completed' THEN 1 END) as answered_calls,
    COUNT(CASE WHEN call_status IN ('no-answer', 'busy', 'failed') THEN 1 END) as missed_calls,
    COALESCE(AVG(CASE WHEN duration > 0 THEN duration END), 0) as avg_duration
FROM transcriptions
WHERE agent_id IN (SELECT id FROM subordinates) AND YEARWEEK(created_at, 1) = YEARWEEK(CURDATE(), 1);

-- name: GetCallStatsByManagerThisMonth :one
WITH RECURSIVE subordinates AS (
    SELECT u.id FROM users u WHERE u.reports_to = ? AND u.company_id = ? AND u.is_active = 1
    UNION ALL
    SELECT u.id FROM users u INNER JOIN subordinates s ON u.reports_to = s.id WHERE u.company_id = ? AND u.is_active = 1
)
SELECT
    COUNT(*) as total_calls,
    COUNT(CASE WHEN call_status = 'completed' THEN 1 END) as answered_calls,
    COUNT(CASE WHEN call_status IN ('no-answer', 'busy', 'failed') THEN 1 END) as missed_calls,
    COALESCE(AVG(CASE WHEN duration > 0 THEN duration END), 0) as avg_duration
FROM transcriptions
WHERE agent_id IN (SELECT id FROM subordinates) AND YEAR(created_at) = YEAR(CURDATE()) AND MONTH(created_at) = MONTH(CURDATE());

-- name: GetCallStatsByManagerRange :one
WITH RECURSIVE subordinates AS (
    SELECT u.id FROM users u WHERE u.reports_to = ? AND u.company_id = ? AND u.is_active = 1
    UNION ALL
    SELECT u.id FROM users u INNER JOIN subordinates s ON u.reports_to = s.id WHERE u.company_id = ? AND u.is_active = 1
)
SELECT
    COUNT(*) as total_calls,
    COUNT(CASE WHEN call_status = 'completed' THEN 1 END) as answered_calls,
    COUNT(CASE WHEN call_status IN ('no-answer', 'busy', 'failed') THEN 1 END) as missed_calls,
    COALESCE(AVG(CASE WHEN duration > 0 THEN duration END), 0) as avg_duration
FROM transcriptions
WHERE agent_id IN (SELECT id FROM subordinates) AND created_at >= ? AND created_at < ?;
```

After adding all queries, run sqlc to regenerate Go code:
```bash
cd /mnt/c/Users/Lihle/.repository/projects/MedContact-Telephony-PWA/api && sqlc generate
```

Critical: Follow research guidance - use separate queries per time filter (not dynamic SQL) because sqlc doesn't support dynamic queries and query planner can't optimize them.
  </action>
  <verify>
Run: `grep -c "GetCallStatsByCompany" api/queries.sql`
Should show 6 (original + 5 time variants).

Run: `grep -c "GetCallStatsByManager" api/queries.sql`
Should show 6 (original + 5 time variants).

Run: `cd /mnt/c/Users/Lihle/.repository/projects/MedContact-Telephony-PWA/api && sqlc generate && echo "sqlc generation successful"`
Should complete without errors.
  </verify>
  <done>
api/queries.sql contains 10 new time-filtered call stat queries (5 admin/support, 5 manager). sqlc has regenerated Go code in api/db/queries.sql.go with type-safe query functions.
  </done>
</task>

<task type="auto">
  <name>Create time-filtered task stat queries for all roles</name>
  <files>api/queries.sql, api/db/queries.sql.go</files>
  <action>
Add time-filtered variants of task stat queries following same pattern as call queries.

Add after existing GetTaskStatsByCompany query in api/queries.sql:

**Admin/Support role queries (company-scoped):**

```sql
-- name: GetTaskStatsByCompanyToday :one
SELECT
    COUNT(*) as total_tasks,
    COUNT(CASE WHEN status = 'pending' THEN 1 END) as pending_tasks,
    COUNT(CASE WHEN status = 'in_progress' THEN 1 END) as in_progress_tasks,
    COUNT(CASE WHEN status = 'completed' THEN 1 END) as completed_tasks,
    COUNT(CASE WHEN type = 'follow-up' THEN 1 END) as follow_up_tasks,
    COUNT(CASE WHEN type = 'callback' THEN 1 END) as callback_tasks,
    COUNT(CASE WHEN status != 'completed' AND due_date IS NOT NULL AND due_date < NOW() THEN 1 END) as overdue_tasks
FROM tasks t
JOIN users u ON t.assigned_to = u.id
WHERE u.company_id = ? AND DATE(t.created_at) = CURDATE();

-- name: GetTaskStatsByCompanyYesterday :one
SELECT
    COUNT(*) as total_tasks,
    COUNT(CASE WHEN status = 'pending' THEN 1 END) as pending_tasks,
    COUNT(CASE WHEN status = 'in_progress' THEN 1 END) as in_progress_tasks,
    COUNT(CASE WHEN status = 'completed' THEN 1 END) as completed_tasks,
    COUNT(CASE WHEN type = 'follow-up' THEN 1 END) as follow_up_tasks,
    COUNT(CASE WHEN type = 'callback' THEN 1 END) as callback_tasks,
    COUNT(CASE WHEN status != 'completed' AND due_date IS NOT NULL AND due_date < NOW() THEN 1 END) as overdue_tasks
FROM tasks t
JOIN users u ON t.assigned_to = u.id
WHERE u.company_id = ? AND DATE(t.created_at) = DATE_SUB(CURDATE(), INTERVAL 1 DAY);

-- name: GetTaskStatsByCompanyThisWeek :one
SELECT
    COUNT(*) as total_tasks,
    COUNT(CASE WHEN status = 'pending' THEN 1 END) as pending_tasks,
    COUNT(CASE WHEN status = 'in_progress' THEN 1 END) as in_progress_tasks,
    COUNT(CASE WHEN status = 'completed' THEN 1 END) as completed_tasks,
    COUNT(CASE WHEN type = 'follow-up' THEN 1 END) as follow_up_tasks,
    COUNT(CASE WHEN type = 'callback' THEN 1 END) as callback_tasks,
    COUNT(CASE WHEN status != 'completed' AND due_date IS NOT NULL AND due_date < NOW() THEN 1 END) as overdue_tasks
FROM tasks t
JOIN users u ON t.assigned_to = u.id
WHERE u.company_id = ? AND YEARWEEK(t.created_at, 1) = YEARWEEK(CURDATE(), 1);

-- name: GetTaskStatsByCompanyThisMonth :one
SELECT
    COUNT(*) as total_tasks,
    COUNT(CASE WHEN status = 'pending' THEN 1 END) as pending_tasks,
    COUNT(CASE WHEN status = 'in_progress' THEN 1 END) as in_progress_tasks,
    COUNT(CASE WHEN status = 'completed' THEN 1 END) as completed_tasks,
    COUNT(CASE WHEN type = 'follow-up' THEN 1 END) as follow_up_tasks,
    COUNT(CASE WHEN type = 'callback' THEN 1 END) as callback_tasks,
    COUNT(CASE WHEN status != 'completed' AND due_date IS NOT NULL AND due_date < NOW() THEN 1 END) as overdue_tasks
FROM tasks t
JOIN users u ON t.assigned_to = u.id
WHERE u.company_id = ? AND YEAR(t.created_at) = YEAR(CURDATE()) AND MONTH(t.created_at) = MONTH(CURDATE());

-- name: GetTaskStatsByCompanyRange :one
SELECT
    COUNT(*) as total_tasks,
    COUNT(CASE WHEN status = 'pending' THEN 1 END) as pending_tasks,
    COUNT(CASE WHEN status = 'in_progress' THEN 1 END) as in_progress_tasks,
    COUNT(CASE WHEN status = 'completed' THEN 1 END) as completed_tasks,
    COUNT(CASE WHEN type = 'follow-up' THEN 1 END) as follow_up_tasks,
    COUNT(CASE WHEN type = 'callback' THEN 1 END) as callback_tasks,
    COUNT(CASE WHEN status != 'completed' AND due_date IS NOT NULL AND due_date < NOW() THEN 1 END) as overdue_tasks
FROM tasks t
JOIN users u ON t.assigned_to = u.id
WHERE u.company_id = ? AND t.created_at >= ? AND t.created_at < ?;
```

**Manager role queries (hierarchy-scoped):**

```sql
-- name: GetTaskStatsByManagerToday :one
WITH RECURSIVE subordinates AS (
    SELECT u.id FROM users u WHERE u.reports_to = ? AND u.company_id = ? AND u.is_active = 1
    UNION ALL
    SELECT u.id FROM users u INNER JOIN subordinates s ON u.reports_to = s.id WHERE u.company_id = ? AND u.is_active = 1
)
SELECT
    COUNT(*) as total_tasks,
    COUNT(CASE WHEN status = 'pending' THEN 1 END) as pending_tasks,
    COUNT(CASE WHEN status = 'in_progress' THEN 1 END) as in_progress_tasks,
    COUNT(CASE WHEN status = 'completed' THEN 1 END) as completed_tasks,
    COUNT(CASE WHEN type = 'follow-up' THEN 1 END) as follow_up_tasks,
    COUNT(CASE WHEN type = 'callback' THEN 1 END) as callback_tasks,
    COUNT(CASE WHEN status != 'completed' AND due_date IS NOT NULL AND due_date < NOW() THEN 1 END) as overdue_tasks
FROM tasks t
WHERE t.assigned_to IN (SELECT id FROM subordinates) AND DATE(t.created_at) = CURDATE();

-- name: GetTaskStatsByManagerYesterday :one
WITH RECURSIVE subordinates AS (
    SELECT u.id FROM users u WHERE u.reports_to = ? AND u.company_id = ? AND u.is_active = 1
    UNION ALL
    SELECT u.id FROM users u INNER JOIN subordinates s ON u.reports_to = s.id WHERE u.company_id = ? AND u.is_active = 1
)
SELECT
    COUNT(*) as total_tasks,
    COUNT(CASE WHEN status = 'pending' THEN 1 END) as pending_tasks,
    COUNT(CASE WHEN status = 'in_progress' THEN 1 END) as in_progress_tasks,
    COUNT(CASE WHEN status = 'completed' THEN 1 END) as completed_tasks,
    COUNT(CASE WHEN type = 'follow-up' THEN 1 END) as follow_up_tasks,
    COUNT(CASE WHEN type = 'callback' THEN 1 END) as callback_tasks,
    COUNT(CASE WHEN status != 'completed' AND due_date IS NOT NULL AND due_date < NOW() THEN 1 END) as overdue_tasks
FROM tasks t
WHERE t.assigned_to IN (SELECT id FROM subordinates) AND DATE(t.created_at) = DATE_SUB(CURDATE(), INTERVAL 1 DAY);

-- name: GetTaskStatsByManagerThisWeek :one
WITH RECURSIVE subordinates AS (
    SELECT u.id FROM users u WHERE u.reports_to = ? AND u.company_id = ? AND u.is_active = 1
    UNION ALL
    SELECT u.id FROM users u INNER JOIN subordinates s ON u.reports_to = s.id WHERE u.company_id = ? AND u.is_active = 1
)
SELECT
    COUNT(*) as total_tasks,
    COUNT(CASE WHEN status = 'pending' THEN 1 END) as pending_tasks,
    COUNT(CASE WHEN status = 'in_progress' THEN 1 END) as in_progress_tasks,
    COUNT(CASE WHEN status = 'completed' THEN 1 END) as completed_tasks,
    COUNT(CASE WHEN type = 'follow-up' THEN 1 END) as follow_up_tasks,
    COUNT(CASE WHEN type = 'callback' THEN 1 END) as callback_tasks,
    COUNT(CASE WHEN status != 'completed' AND due_date IS NOT NULL AND due_date < NOW() THEN 1 END) as overdue_tasks
FROM tasks t
WHERE t.assigned_to IN (SELECT id FROM subordinates) AND YEARWEEK(t.created_at, 1) = YEARWEEK(CURDATE(), 1);

-- name: GetTaskStatsByManagerThisMonth :one
WITH RECURSIVE subordinates AS (
    SELECT u.id FROM users u WHERE u.reports_to = ? AND u.company_id = ? AND u.is_active = 1
    UNION ALL
    SELECT u.id FROM users u INNER JOIN subordinates s ON u.reports_to = s.id WHERE u.company_id = ? AND u.is_active = 1
)
SELECT
    COUNT(*) as total_tasks,
    COUNT(CASE WHEN status = 'pending' THEN 1 END) as pending_tasks,
    COUNT(CASE WHEN status = 'in_progress' THEN 1 END) as in_progress_tasks,
    COUNT(CASE WHEN status = 'completed' THEN 1 END) as completed_tasks,
    COUNT(CASE WHEN type = 'follow-up' THEN 1 END) as follow_up_tasks,
    COUNT(CASE WHEN type = 'callback' THEN 1 END) as callback_tasks,
    COUNT(CASE WHEN status != 'completed' AND due_date IS NOT NULL AND due_date < NOW() THEN 1 END) as overdue_tasks
FROM tasks t
WHERE t.assigned_to IN (SELECT id FROM subordinates) AND YEAR(t.created_at) = YEAR(CURDATE()) AND MONTH(t.created_at) = MONTH(CURDATE());

-- name: GetTaskStatsByManagerRange :one
WITH RECURSIVE subordinates AS (
    SELECT u.id FROM users u WHERE u.reports_to = ? AND u.company_id = ? AND u.is_active = 1
    UNION ALL
    SELECT u.id FROM users u INNER JOIN subordinates s ON u.reports_to = s.id WHERE u.company_id = ? AND u.is_active = 1
)
SELECT
    COUNT(*) as total_tasks,
    COUNT(CASE WHEN status = 'pending' THEN 1 END) as pending_tasks,
    COUNT(CASE WHEN status = 'in_progress' THEN 1 END) as in_progress_tasks,
    COUNT(CASE WHEN status = 'completed' THEN 1 END) as completed_tasks,
    COUNT(CASE WHEN type = 'follow-up' THEN 1 END) as follow_up_tasks,
    COUNT(CASE WHEN type = 'callback' THEN 1 END) as callback_tasks,
    COUNT(CASE WHEN status != 'completed' AND due_date IS NOT NULL AND due_date < NOW() THEN 1 END) as overdue_tasks
FROM tasks t
WHERE t.assigned_to IN (SELECT id FROM subordinates) AND t.created_at >= ? AND t.created_at < ?;
```

After adding, regenerate Go code:
```bash
cd /mnt/c/Users/Lihle/.repository/projects/MedContact-Telephony-PWA/api && sqlc generate
```
  </action>
  <verify>
Run: `grep -c "GetTaskStatsByCompany" api/queries.sql`
Should show 6 (original + 5 time variants).

Run: `grep -c "GetTaskStatsByManager" api/queries.sql`
Should show 6 (original + 5 time variants).

Run: `cd /mnt/c/Users/Lihle/.repository/projects/MedContact-Telephony-PWA/api && sqlc generate && echo "sqlc success"`
Should complete without errors, generating type-safe Go query functions.
  </verify>
  <done>
api/queries.sql contains 10 new time-filtered task stat queries (5 admin/support, 5 manager). sqlc has regenerated Go code in api/db/queries.sql.go. Database layer is ready for time-filtered metrics.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. Check indexes exist:
   ```bash
   grep "idx_transcriptions_company_created\|idx_transcriptions_agent_created\|idx_tasks_assigned_created" /mnt/c/Users/Lihle/.repository/projects/MedContact-Telephony-PWA/api/schema.sql
   ```

2. Count query variants:
   ```bash
   grep -c "GetCallStatsByCompany" /mnt/c/Users/Lihle/.repository/projects/MedContact-Telephony-PWA/api/queries.sql  # Should be 6
   grep -c "GetTaskStatsByManager" /mnt/c/Users/Lihle/.repository/projects/MedContact-Telephony-PWA/api/queries.sql  # Should be 6
   ```

3. Verify sqlc generation succeeded:
   ```bash
   cd /mnt/c/Users/Lihle/.repository/projects/MedContact-Telephony-PWA/api && sqlc generate
   ls -la db/queries.sql.go  # Should show recent timestamp
   ```
</verification>

<success_criteria>
- [ ] api/schema.sql contains 3 new composite indexes on transcriptions and tasks tables
- [ ] api/queries.sql contains 20 new time-filtered queries (10 call, 10 task across admin/manager roles)
- [ ] sqlc code generation completes successfully
- [ ] api/db/queries.sql.go contains generated Go functions for all new queries
- [ ] Queries follow research pattern of separate queries per time filter (not dynamic SQL)
- [ ] All role-based scoping from Phase 2 is preserved in new queries
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-metrics-time-filtering/03-01-SUMMARY.md`
</output>
